# ISagent——PRD产品需求文档

## 文档信息

| 字段 | 内容 |
|------|------|
| 版本 | V1.0 |
| 日期 | 2026-07-29 |
| 作者 | Mavis（AI辅助生成） |
| 状态 | 待评审 |

---

## 基础产品概述

### 产品定位与价值主张

| 维度 | 内容 |
|------|------|
| **产品名称** | ISagent（Information Sharing Agent） |
| **产品定位** | 校园信息差共享AI助手——让新生和外来人员快速获取校园内部一手经验 |
| **核心价值** | 打破信息壁垒，通过用户共创+AI检索的方式，把分散的校园经验沉淀为可查询的知识库 |
| **痛点** | 新生或外来人员无法快速了解学校内部的各种信息（如哪个窗口饭菜好吃、理发店推荐、校门选择等），只能靠口口相传或自己摸索 |
| **目标用户** | 新生、外来作业人员 |
| **核心场景** | 路况指引、社区评价（校园各类生活服务评价）、数字资源查询 |

---

### MVP 范围与边界

#### 优先级矩阵

| 名称 | 级别 | 原因 |
|------|------|------|
| 信息差上传与审核 | P0 MVP | 核心内容生产流程 |
| RAG智能问答 | P0 MVP | 核心用户体验 |
| 结构化输出 | P0 MVP | 前端解析必需 |
| 社区评价详情页 | P1 | 增强信息展示 |
| 并发支持（Redis） | P2 | 后期扩展 |

#### MVP边界说明

1. **功能边界**
   - ✅ 用户上传信息差 → 人工审核 → 入库
   - ✅ 用户提问 → RAG检索 → LLM生成 → 结构化输出
   - ❌ 不执行实际操作（如预约、支付、导航）
   - ❌ 不接入第三方评价系统

2. **技术边界**
   - ✅ 单Agent + RAG架构
   - ❌ 暂不考虑多Agent协作
   - ❌ 暂不考虑复杂OCR识别

3. **数据边界**
   - ✅ 支持 docx、pdf、txt 文档导入
   - ✅ 支持用户上传文本内容
   - ❌ 暂不支持图片/音视频内容

---

## 常规功能需求

### 业务流程图

#### 整体业务流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  信息上传流程  │     │  问答流程    │     │  后台管理流程  │
└─────────────┘     └─────────────┘     └─────────────┘
```

#### 子流程1：信息上传与审核

```
用户输入信息差内容
       ↓
文本预处理（清洗、敏感词过滤）
       ↓
管理员审核（通过/驳回/修改）
       ↓
┌─────────────┐
│  审核通过    │────→ 存入RAG知识库（Chroma）
└─────────────┘
       ↓
┌─────────────┐
│  审核驳回    │────→ 返回用户修改意见
└─────────────┘
```

#### 子流程2：智能问答（核心流程）

```
1. 用户端：输入关于校园的自然语言提问
       ↓
2. 语义识别：LLM解析用户意图 + 提取查询关键词
       ↓
3. 模块分流：判定为信息查询类需求
       ↓
4. RAG 检索：向量库检索匹配的历史经验、政策、案例
       ↓
5. 素材输出：获取 Top-K 条匹配的知识库文本
       ↓
6. 内容融合：合并 RAG 检索文本，构建完整上下文
       ↓
7. LLM 推理：将上下文送入大模型生成专业答复
       ↓
8. 结构化输出：整理为 JSON 字段
       ↓
9. 结果输出：返回给前端解析展示
```

#### 子流程3：后台管理（管理员）

```
查看待审核信息列表
       ↓
审核操作（通过/驳回/修改）
       ↓
查看已入库知识库
       ↓
支持手动增删改查
```

---

### 功能模块详细说明

#### 模块1：用户问答模块（P0）

| 功能点 | 描述 | 输入 | 输出 |
|--------|------|------|------|
| 自然语言提问 | 用户用自然语言描述问题 | 文本 | - |
| 意图识别 | 解析问题类型和关键词 | 文本 | 结构化意图标签 |
| RAG检索 | 从向量库检索相关内容 | 查询向量 | Top-K 相关文本块 |
| 答案生成 | LLM基于上下文生成回答 | 上下文 | 结构化JSON |
| 流式输出 | 支持打字机效果 | - | SSE/流式响应 |

#### 模块2：信息上传模块（P0）

| 功能点 | 描述 | 输入 | 输出 |
|--------|------|------|------|
| 提交信息差 | 用户提交自己知道的校园信息 | 标题 + 正文 + 分类标签 | 提交成功/失败 |
| 我的上传 | 查看自己提交的历史记录 | - | 上传列表 |
| 审核状态 | 查看审核进度 | - | 状态（待审/通过/驳回） |

#### 模块3：管理员审核模块（P0）

| 功能点 | 描述 | 输入 | 输出 |
|--------|------|------|------|
| 待审核列表 | 查看所有待审核内容 | - | 分页列表 |
| 审核操作 | 通过/驳回/修改后通过 | 审核结果 + 意见 | 更新状态 |
| 知识库管理 | 手动增删改查知识库 | - | 操作结果 |

#### 模块4：知识库管理模块（P1）

| 功能点 | 描述 | 输入 | 输出 |
|--------|------|------|------|
| 文档导入 | 导入 docx/pdf/txt 文档 | 文件 | 导入结果 |
| 文本录入 | 手动录入知识条目 | 标题 + 内容 | 录入结果 |
| 向量重建 | 手动触发重建索引 | - | 重建结果 |

---

### API 接口设计

#### 对外API（供前端调用）

##### 1. 问答接口

```
POST /api/v1/chat

Request:
{
  "user_id": "string",
  "question": "string",
  "session_id": "string"  // 可选，用于上下文连贯
}

Response (流式):
{
  "code": 0,
  "message": "success",
  "data": {
    "session_id": "string",
    "answer": {
      "summary": "string",           // 回答摘要
      "details": [...],              // 详细建议列表
      "sources": [...],               // 参考来源
      "confidence": 0.85              // 置信度
    }
  }
}
```

##### 2. 信息上传接口

```
POST /api/v1/knowledge/submit

Request:
{
  "user_id": "string",
  "title": "string",
  "content": "string",
  "category": "string"  // 分类标签
}

Response:
{
  "code": 0,
  "message": "success",
  "data": {
    "submission_id": "string",
    "status": "pending_review"
  }
}
```

##### 3. 审核接口（管理员）

```
POST /api/v1/admin/review

Request:
{
  "submission_id": "string",
  "action": "approve|reject|modify",
  "review_comment": "string",  // 驳回/修改意见
  "modified_content": "string" // 修改后的内容（仅modify时）
}

Response:
{
  "code": 0,
  "message": "success"
}
```

#### 内部API（Agent ↔ RAG ↔ LLM）

##### RAG检索接口

```python
# LangChain Retriever
retriever = vectorstore.as_retriever(
    search_kwargs={"k": 5}  # Top-5 检索
)
```

##### LLM调用接口

```python
# MiniMax API 调用
response = minimax.chat.completions.create(
    model="minimax-v2.7",
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": user_prompt}
    ],
    stream=True
)
```

---

### 异常处理

| 异常场景 | 处理策略 |
|----------|----------|
| LLM服务不可用 | 返回兜底回复："服务暂时不可用，请稍后再试" |
| RAG检索为空 | 提示用户换个问法，或引导上传信息差 |
| 上传内容含敏感词 | 直接拒绝，返回敏感词提示 |
| 上传内容过长 | 截断或分段处理，提示用户 |
| 管理员审核超时 | 保留待审核状态，发送提醒 |

---

### 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 响应时间 | ≤5秒 | 首token响应时间 |
| 输出方式 | 流式输出 | SSE实时返回 |
| 并发能力 | 单用户测试版 | 后期引入Redis扩展 |
| 可用性 | 99% | MVP阶段目标 |

---

## AI专属模块

### Agent Workflow 设计

#### 单Agent架构

```
┌──────────────────────────────────────────────────────────┐
│                      User Query                          │
│              "学校哪个理发店比较好？"                       │
└──────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────┐
│                    Intent Classifier                      │
│              判断意图：信息查询 / 投诉 / 闲聊              │
└──────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────┐
│                     Retriever                            │
│         从Chroma向量库检索Top-5相关知识块                  │
└──────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────┐
│                    Context Builder                       │
│              组装: 检索结果 + 历史对话 + 用户画像           │
└──────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────┐
│                   MiniMax LLM (v2.7)                      │
│              基于上下文生成结构化回答                       │
└──────────────────────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────────────────────────────┐
│                   Output Formatter                        │
│              转换为JSON格式返回前端                        │
└──────────────────────────────────────────────────────────┘
```

#### 多阶段处理流程

| 阶段 | 组件 | 职责 |
|------|------|------|
| 1 | IntentClassifier | 判断用户意图，过滤无效/敏感请求 |
| 2 | Retriever | 从Chroma检索Top-K相关知识 |
| 3 | ContextBuilder | 组装检索结果 + few-shot示例 |
| 4 | LLMGenerator | 生成自然语言回答 |
| 5 | OutputFormatter | 转换为结构化JSON |

---

### Prompt 需求规范

#### System Prompt（系统提示词）

```markdown
# 角色定义
你是一个校园信息助手，名为ISagent，专门帮助新生和外来人员了解校园内部的各种信息。

# 能力范围
- 回答关于校园生活的问题（食堂、超市、理发店、校门、停车等）
- 提供基于用户真实经验的信息差分享
- 推荐评价和选择建议

# 输出格式
必须返回JSON格式，包含以下字段：
{
  "summary": "string",      // 一句话总结回答
  "details": [              // 详细建议列表
    {
      "title": "string",    // 建议标题
      "content": "string",  // 具体内容
      "source": "string"    // 参考来源（可选）
    }
  ],
  "confidence": 0.0-1.0,    // 置信度
  "fallback": "string"      // 置信度低时的兜底回复
}

# 约束规则
1. 只基于检索到的知识回答，不编造信息
2. 如果知识库中没有相关信息，坦诚告知用户
3. 回答需要简洁、有条理，便于用户快速获取关键信息
4. 保持中立，不偏袒任何特定商家或服务
5. 涉及重要决策（如安全问题）时，提醒用户多方核实

# Few-shot 示例
用户: "学校附近有什么好吃的推荐？"
回答: {
  "summary": "根据校园用户评价，以下是几个受欢迎的餐厅推荐",
  "details": [
    {"title": "西门火锅店", "content": "性价比高，适合聚餐", "source": "用户评价"},
    {"title": "食堂三楼", "content": "菜品丰富，川菜窗口推荐", "source": "用户评价"}
  ],
  "confidence": 0.85,
  "fallback": null
}
```

#### User Prompt（用户提示词）

```markdown
# 用户问题
{user_question}

# 检索到的相关知识
{retrieved_knowledge}

# 对话历史（最近3轮）
{conversation_history}

# 用户画像（可选）
{user_profile}

请基于以上信息，用JSON格式回答用户问题。
```

#### Intent Classifier Prompt

```markdown
# 任务
判断用户输入的意图类型。

# 意图类型
1. INFORMATION_QUERY - 信息查询（食堂、超市、停车场等）
2. EVALUATION_REQUEST - 评价请求（哪家好、哪个推荐）
3. NAVIGATION_REQUEST - 导航请求（在哪、怎么走）
4. COMPLAINT - 投诉反馈
5. INFORMATION_SHARE - 信息分享（用户上传知识）
6. GREETING - 问候闲聊
7. OTHER - 其他

# 输出格式
只输出意图类型编号和简短理由，用JSON格式：
{"intent": "INTENT_TYPE", "reason": "简短理由"}
```

---

### 评估标准与测试数据集

#### 评估维度

| 维度 | 指标 | 目标值 | 说明 |
|------|------|--------|------|
| **相关性** | Retrieval Hit Rate | ≥80% | Top-5召回率 |
| **准确性** | Answer Accuracy | ≥75% | 回答正确率（人工评判） |
| **完整性** | Coverage Rate | ≥70% | 覆盖用户问题比例 |
| **响应速度** | Response Time | ≤5s | P95响应时间 |
| **格式正确率** | JSON Valid Rate | ≥95% | 输出JSON格式正确率 |

#### 测试数据集

| 类型 | 数量 | 来源 |
|------|------|------|
| 问答应例 | 50条 | 人工构造 + AI辅助生成 |
| 分类测试 | 20条 | 覆盖7种意图类型 |
| 边界测试 | 10条 | 空输入、敏感词、长文本等 |

#### 测试用例示例

```json
[
  {
    "id": 1,
    "category": "EVALUATION_REQUEST",
    "question": "学校哪个理发店比较推荐？",
    "expected_answer_type": "recommendation_list"
  },
  {
    "id": 2,
    "category": "NAVIGATION_REQUEST",
    "question": "新生报到应该走哪个校门？",
    "expected_answer_type": "guidance"
  },
  {
    "id": 3,
    "category": "INFORMATION_QUERY",
    "question": "学校超市几点关门？",
    "expected_answer_type": "factual_answer"
  }
]
```

#### 评估流程

```
1. 准备测试数据集（50条）
       ↓
2. 批量运行问答
       ↓
3. 人工评分（准确性、相关性）
       ↓
4. 统计各项指标
       ↓
5. 生成评估报告
       ↓
6. 根据反馈迭代Prompt
```

---

## 数据与接口需求

### 上下文构建策略

#### 输入数据结构

```json
{
  "user": {
    "user_id": "string",
    "role": "student|visitor|admin",
    "query_count": 10
  },
  "conversation": {
    "session_id": "string",
    "history": [
      {"role": "user", "content": "...", "timestamp": "..."},
      {"role": "assistant", "content": "...", "timestamp": "..."}
    ]
  },
  "current_query": {
    "text": "string",
    "intent": "INFORMATION_QUERY",
    "keywords": ["校园", "食堂", "推荐"]
  },
  "retrieved_knowledge": [
    {
      "content": "string",
      "source": "user_submission|admin_import|document",
      "score": 0.85,
      "metadata": {}
    }
  ]
}
```

#### 输出数据结构

```json
{
  "answer": {
    "summary": "string",
    "details": [
      {
        "title": "string",
        "content": "string",
        "source": "string",
        "source_id": "string"
      }
    ],
    "confidence": 0.85,
    "intent": "EVALUATION_REQUEST",
    "suggestions": ["相关问题1", "相关问题2"]
  },
  "meta": {
    "tokens_used": 500,
    "retrieval_time_ms": 120,
    "total_time_ms": 3200
  }
}
```

### 数据存储方案

| 数据类型 | 存储方案 | 说明 |
|----------|----------|------|
| 用户上传原始内容 | PostgreSQL / MySQL | 结构化存储，含审核状态 |
| 知识库文本 | Chroma | 向量存储，用于RAG检索 |
| 会话历史 | PostgreSQL / Redis | 短期会话 + 长期记忆 |
| 用户信息 | PostgreSQL / MySQL | 用户注册、权限管理 |
| 评估日志 | 文件 / 数据库 | 评估结果、错误日志 |

### 第三方服务依赖

| 服务 | 用途 | 优先级 |
|------|------|--------|
| MiniMax API | LLM能力 | P0 |
| Chroma | 向量数据库 | P0 |
| FastAPI | Web服务 | P0 |
| LangChain | Agent框架 | P0 |
| Redis | 会话缓存/并发（后期） | P2 |

---

## 风险管控与人工干预

### 不可控风险与边界

| 风险类型 | 具体风险 | 缓解措施 |
|----------|----------|----------|
| **模型风险** | LLM产生幻觉/编造信息 | 强制RAG检索兜底，不回答无检索结果的问题 |
| **输出风险** | 返回非JSON格式 | 增加输出校验和重试机制 |
| **内容风险** | 用户上传敏感/错误信息 | 人工审核 + 敏感词过滤 |
| **隐私风险** | 用户信息泄露 | 最小化数据收集，敏感字段加密 |
| **可用性风险** | MiniMax/Chroma服务不可用 | 降级兜底策略 + 监控告警 |
| **版本风险** | API版本升级不兼容 | 封装API调用，记录版本号 |

### 人工干预点（Human-in-the-Loop）

| 节点 | 干预类型 | 说明 |
|------|----------|------|
| **信息上传审核** | ✅ 必须 | 所有用户上传内容必须经过人工审核 |
| **知识库导入** | ✅ 必须 | 文档导入需管理员确认 |
| **系统Prompt调整** | ✅ 建议 | Prompt重大变更需评审 |
| **答案质量抽检** | ✅ 建议 | 定期抽检回答质量 |

### 边界情况处理

| 场景 | 处理策略 |
|------|----------|
| RAG检索为空 | 返回："知识库中暂无相关信息，欢迎您分享这个信息差~" |
| 置信度 < 0.5 | 使用fallback字段，提供模糊/保守回答 |
| 用户上传重复内容 | 提示用户已有类似内容，可选择补充 |
| 并发超限 | 排队处理，或返回"服务繁忙，请稍后再试" |
| LLM超时 | 重试1次，仍失败返回兜底回复 |

---

## 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层                                 │
│                   (Web / 小程序)                              │
└─────────────────────────────────────────────────────────────┘
                            ↓ HTTP/SSE
┌─────────────────────────────────────────────────────────────┐
│                       FastAPI 网关                            │
│                   (路由、认证、限流)                           │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     LangChain Agent                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ Intent      │  │ Retriever   │  │ LLM         │          │
│  │ Classifier  │→ │ (Chroma)    │→ │ Generator   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐
│ MiniMax LLM │  │   Chroma    │  │     PostgreSQL          │
│  (v2.7)     │  │  (向量库)   │  │  (用户、审核、会话)       │
└─────────────┘  └─────────────┘  └─────────────────────────┘
                            ↓ (可选)
                      ┌─────────────┐
                      │   Redis     │
                      │  (后期并发)  │
                      └─────────────┘
```

---

## 开发里程碑

| 阶段 | 目标 | 交付物 | 预计工时 |
|------|------|--------|----------|
| **M1** | 核心问答流程跑通 | Agent + RAG + LLM 串联 | 3-5天 |
| **M2** | 用户上传 + 审核功能 | 前后端完整流程 | 2-3天 |
| **M3** | Prompt优化 + 评估体系 | 50条测试用例 + 评估报告 | 2天 |
| **M4** | 性能优化 + 边界处理 | 响应时间达标 + 异常处理完善 | 2天 |
| **M5** | 部署上线 | 线上可用 | 1天 |

---

## 附录

### 术语表

| 术语 | 解释 |
|------|------|
| ISagent | Information Sharing Agent，校园信息共享AI助手 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Chroma | 开源向量数据库 |
| 流式输出 | SSE/Server-Sent Events，边生成边返回 |
| Human-in-the-loop | 人工干预机制 |

### 参考资料

- LangChain Documentation
- MiniMax API Reference
- Chroma Quickstart Guide

---

## 文档变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2026-07-29 | 初始版本，AI辅助生成 | Mavis |
